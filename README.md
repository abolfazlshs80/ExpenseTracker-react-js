# Expense Tracker

یک برنامه مدیریت هزینه‌های شخصی ساخته شده با React، Vite و TailwindCSS.

## ویژگی‌ها

- **افزودن تراکنش‌ها**: ثبت درآمد و هزینه با دسته‌بندی
- **نمایش لیست تراکنش‌ها**: مشاهده تمام تراکنش‌های ثبت شده
- **نمودارهای آماری**: نمودار دایره‌ای، میله‌ای و خطی برای تحلیل هزینه‌ها
- **مدیریت دسته‌بندی‌ها**: دسته‌بندی‌های پیش‌فرض و امکان افزودن دسته‌بندی جدید
- **ذخیره‌سازی محلی**: داده‌ها در localStorage مرورگر نگهداری می‌شوند
- **ریسپانسیو**: سازگار با موبایل و دسکتاپ

## تکنولوژی‌ها

- **React 19** - کتابخانه UI
- **Vite** - ابزار بیلد سریع
- **TailwindCSS 4** - فریم‌ورک CSS
- **React Router DOM** - مسیریابی
- **Recharts** - کتابخانه نمودارها
- **React Context API** - مدیریت حالت (State Management)
- **ESLint** - لنتر کد

## پیش‌نیازها

- Node.js (نسخه 18 یا بالاتر)
- npm یا yarn

## نصب و اجرا

```bash
# کلون کردن مخزن
git clone <repository-url>

# ورود به پوشه پروژه
cd expense-tracker

# نصب وابستگی‌ها
npm install

# اجرای محیط توسعه
npm run dev
```

سپس در مرورگر به آدرس `http://localhost:5173` بروید.

## اسکریپت‌های موجود

| دستور | توضیح |
|--------|-------|
| `npm run dev` | اجرای سرور توسعه |
| `npm run build` | بیلد برای تولید |
| `npm run lint` | بررسی کد با ESLint |
| `npm run preview` | پیش‌نمایش بیلد تولیدی |

## ساختار پروژه

```
src/
├── components/
│   ├── Bills/          # کامپوننت‌های مدیریت صورت‌حساب
│   │   ├── BillsContext.jsx   # Context و Provider برای تراکنش‌ها
│   │   ├── BillsLayout.jsx    # چیدمان بخش صورت‌حساب
│   │   ├── EnterBills.jsx     # فرم ورود تراکنش
│   │   ├── ShowBills.jsx      # نمایش لیست تراکنش‌ها
│   │   └── BillsList.jsx      # لیست تراکنش‌ها
│   ├── Layout/         # کامپوننت‌های چیدمان اصلی
│   ├── MenuBar/        # منوی ناوبری کناری
│   ├── NavBar/         # نوار ناوبری بالا
│   ├── Tables/         # جداول و نمودارها
│   │   ├── Tables.jsx       # جداول داده
│   │   ├── PieCharts.jsx    # نمودار دایره‌ای
│   │   ├── BarCharts.jsx    # نمودار میله‌ای
│   │   └── LineCharts.jsx   # نمودار خطی
│   ├── Inputs/         # کامپوننت‌های ورودی
│   └── Button/         # کامپوننت دکمه
├── images/             # تصاویر و آیکون‌ها
├── App.jsx             # کامپوننت اصلی
├── main.jsx            # نقطه ورودی
└── index.css           # استایل‌های سراسری
```

## نحوه کارکرد

### مدیریت حالت (State Management)
از React Context API استفاده شده است. `BillsContext` تراکنش‌ها و دسته‌بندی‌ها را مدیریت کرده و در localStorage ذخیره می‌کند.

### مسیرها
برنامه از React Router برای مسیریابی استفاده می‌کند.

### استایل‌دهی
TailwindCSS برای استایل‌دهی به کار رفته است.

## توسعه‌دهنده

**فرویش کریمی** - تمرین React Context (HW-L03-03)

## لایسنس

این پروژه برای اهداف آموزشی ساخته شده است.